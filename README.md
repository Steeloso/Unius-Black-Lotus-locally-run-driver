If you plan on building it yourself, install npm on the directory
"WebPage crude files" folder is NOT required, this is just if you want to run it as an webpage locally (by hosting it yourself), so you have that choice
